name: esc_pos_bluetooth
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer.
version: 0.2.8
homepage: https://github.com/andrey-ushakov/esc_pos_bluetooth

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.24.1
  esc_pos_utils: ^0.4.5
  flutter_bluetooth_basic:
    git:
      url: https://github.com/ghprod/flutter_bluetooth_basic.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
